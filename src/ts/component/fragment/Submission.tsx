import {SimpleComponent} from "../../core/ComposeComponent";

class Submission extends SimpleComponent {
    public render() {
        return (
            <div>Submission</div>
        );
    }
}

export default Submission